__pycache__/
*.pyc
*.egg-info/
/build/
/dist/
.pytest_cache/
.venv/

# --- Data & generated artifacts ---
# Dataset SOURCE data lives OUTSIDE the repo, under the shared $LONGEVITY_DATA_ROOT
# (see data/DATASETS.md). EVERYTHING under generated/ is a local, regenerable build
# output (reconstructed metadata, betas.csv, the Versioned Score Table, analysis
# results) and is NEVER committed. The generator scripts are code and live in
# tools/, which IS tracked.
generated/

# Ad-hoc / reproduction run output dirs (repo-root)
/out/
/reliage_out/
/out_repro/

# Legacy download dir + stray archives
/data/gse55763/
/data/*.parquet
/data/*.csv
*.tar.gz
